#include <stdio.h>
#include <stdlib.h>
#include <time.h>


void fiilArray(int arr[], int dim); // Caricamento dell'array con i valori casuali con rand()
void bubble_sort(int arr[], int dim); // Ordinamento di un array con il metodo bubble sort
void printArray(const int arr[], int dim); // Stampa un array